# {{ project_name }}

> 🎯 Generated from [martgra/typescript_template](https://github.com/martgra/typescript_template)
>
> ```bash
> # Generate your own project with Copier
> uvx copier copy gh:martgra/typescript_template my-project
> ```

{{ project_description }}

[![Copier](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/copier-org/copier/master/img/badge/badge-grayscale-inverted-border-orange.json)](https://github.com/copier-org/copier)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](./LICENSE)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.2%2B-blue)](https://www.typescriptlang.org/)
[![Bun](https://img.shields.io/badge/bun-latest-black)](https://bun.sh)

## ✨ Features

- 🚀 **TypeScript** - Type-safe development{% if typescript_strict %} with strict mode{% endif %}
- 📦 **Bun** - Fast package management and runtime{% if project_type == 'spa' %}
- ⚡ **Vite** - Lightning-fast dev server and bundler
- ⚛️ **React 19** - Component-based UI{% if use_tailwind %}
- 🌬️ **Tailwind CSS 4** - Utility-first styling{% endif %}{% endif %}
- 🎨 **Biome** - Lint, format, and import sorting for code (JS/TS/JSON)
- 📝 **Prettier** - Formatting for Markdown & YAML
- 🔍 **Knip** - Detect unused code and dependencies
- 🪝 **Husky** - Two-tier git hooks (commit + push)
- 🔐 **Secret Detection** - Prevent committing secrets{% if use_github_actions %}
- ⚙️ **GitHub Actions** - Automated CI/CD{% endif %}{% if use_devcontainer %}
- 🐳 **Dev Container** - Consistent development environment{% endif %}{% if use_agents %}
- 🤖 **AGENTS.md** - AI agent conventions for Claude Code{% endif %}

## Quick Start

```bash
# Install dependencies
bun install

# Set up git hooks
bun run prepare
{% if project_type == 'spa' %}
# Start the dev server (http://localhost:5173)
bun run dev

# Build for production (outputs to dist/)
bun run build

# Preview the production build
bun run preview
```{% else %}
# Start development with watch mode
bun run dev

# Build for production
bun run build

# Run production build
bun start
```{% endif %}

## 🛠️ Development Tools

### Linting & Formatting

```bash
# Run Biome (lint + format + organize imports for code)
bun run check
bun run lint
bun run lint:fix

# Check/fix formatting (Biome for code, Prettier for docs)
bun run format
bun run format:check

# Find unused code/dependencies
bun run knip
```

### Git Hooks

**Pre-commit** (fast, runs on staged files):

- Scan for secrets with secretlint
- Run Biome (lint, format, organize imports)
- Format Markdown & YAML with Prettier

**Pre-push** (slow, runs on full project):

- Build the project (`bun run build`)
- Check for unused code and dependencies (`bun run knip`)

**Note:** Changes are **not auto-staged**. Review, stage, and commit again if hooks make changes.

## 📁 Project Structure

```
.{% if use_devcontainer %}
├── .devcontainer/       # Dev container configuration{% endif %}{% if use_github_actions %}
├── .github/
│   └── workflows/       # GitHub Actions CI/CD{% endif %}
├── .husky/              # Git hooks{% if use_vscode %}
├── .vscode/             # VSCode settings{% endif %}{% if project_type == 'spa' %}
├── index.html           # Vite entry point
├── vite.config.ts       # Vite configuration
├── src/
│   ├── main.tsx         # React bootstrap
│   ├── App.tsx          # Root component
│   └── index.css        # Global styles
├── dist/                # Build output{% else %}
├── src/
│   └── index.ts         # Entry point
├── dist/                # Build output{% endif %}
├── CONTRIBUTING.md      # Contribution guidelines
├── CHANGELOG.md         # Version history
├── LICENSE              # MIT License file
└── package.json
```

## ⚙️ Configuration Files

- **`biome.json`** - Lint, format, and import sort rules (JS/TS/JSON)
- **`.prettierrc`** - Formatting rules for Markdown & YAML
- **`.editorconfig`** - Editor configuration for consistency
- **`knip.json`** - Unused code and dependency detection
- **`.secretlintrc.json`** - Secret detection rules
- **`tsconfig.json`** - TypeScript compiler options{% if typescript_strict %} (strict mode){% endif %}{% if project_type == 'spa' %}
- **`vite.config.ts`** - Vite build and dev server configuration{% endif %}

## 🤝 Contributing

See [CONTRIBUTING.md](./CONTRIBUTING.md) for guidelines.

## 📝 License

MIT - See [LICENSE](./LICENSE) file for details.
