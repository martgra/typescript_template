# Answers to copier template questions
# This file is used by Copier to track template version and user's answers

_src_path: {{ _copier_conf.src_path }}
_commit: {{ _copier_conf.vcs_ref_hash }}

project_name: "{{ project_name }}"
project_description: "{{ project_description }}"
author_name: "{{ author_name }}"
author_email: "{{ author_email }}"
use_git: {{ use_git }}
use_devcontainer: {{ use_devcontainer }}
setup_husky: {{ setup_husky }}
typescript_strict: {{ typescript_strict }}
package_manager: "{{ package_manager }}"
